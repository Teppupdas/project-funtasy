extends Node

#@onready var platforma1 = $platformaPiwniczna1
#@onready var platforma2 = $platformaPiwniczna2
#
#
#
#
#
#func _ready() -> void:
	#chujek()
#
#
#func _process(delta: float) -> void:
	#pass
#
#
#func chujek():
	#var move_tween = create_tween()
	#move_tween.tween_property(platforma1, "position", Vector3(185,-5,-14), 5).set_trans(Tween.TRANS_SINE).set_delay(3)
	#move_tween.tween_property(platforma1, "position", Vector3(190,-5,-14), 5).set_trans(Tween.TRANS_SINE).set_delay(3)
	#await get_tree().create_timer(16).timeout
	#chujek()
